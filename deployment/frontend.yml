format_version: 10
pipelines:
  frontend:
    group: App-Deployments
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-a15a35c:
        git: https://github.com/spatil-Devops/expense-shell.git
        shallow_clone: true
        auto_update: true
        branch: main
    stages:
    - DEV:
        fetch_materials: true
        jobs:
          ansible:
            timeout: 0
            tasks:
            - exec:
                arguments:
                - -i
                - frontend-dev.sdevopsp25.site,
                - -e
                - ansible_user=ec2-user
                - -e
                - ansible_password=DevOps321
                - -e
                - env=dev
                - -e
                - role_name=frontend
                - expense.yml
                command: ansible-playbook
    - QA:
        fetch_materials: true
        approval:
          type: manual
        jobs:
          ansible:
            timeout: 0
            tasks:
              - exec:
                  arguments:
                    - -l
                  command: ls
                  run_if: passed
    - Stage:
        fetch_materials: true
        approval:
          type: manual
        jobs:
          ansible:
            timeout: 0
            tasks:
              - exec:
                  arguments:
                    - -l
                  command: ls
                  run_if: passed
    - Prod:
        fetch_materials: true
        approval:
          type: manual
        jobs:
          ansible:
            timeout: 0
            tasks:
              - exec:
                  arguments:
                    - -l
                  command: ls
                  run_if: passed